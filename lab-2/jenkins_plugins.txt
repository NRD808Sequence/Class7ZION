# =============================================================================
# jenkins_plugins.txt — Vandelay Lab-2 Jenkins Plugin Manifest
#
# Format: <plugin-id>:<version>
# Use "latest" to always pull the newest compatible version.
# Pin a version (e.g. git:5.2.0) to lock for reproducibility.
#
# Install with: ./jenkins_install_plugins.sh
# =============================================================================

# ---------------------------------------------------------------------------
# Pipeline core — declarative syntax, stages, when{}, input, post{}
# ---------------------------------------------------------------------------
workflow-aggregator:latest

# ---------------------------------------------------------------------------
# Source control
# ---------------------------------------------------------------------------
git:latest

# ---------------------------------------------------------------------------
# Credentials & secrets injection
# Provides: credentials('...') in environment{} block → TF_VAR_db_password
# ---------------------------------------------------------------------------
credentials:latest
credentials-binding:latest

# ---------------------------------------------------------------------------
# Console / UX
# ---------------------------------------------------------------------------
timestamper:latest          # timestamps() option — prefix every log line
ansicolor:latest            # coloured terraform/shell output in console

# ---------------------------------------------------------------------------
# Build management
# ---------------------------------------------------------------------------
ws-cleanup:latest           # cleanWs() in post{} — purges .terraform/ between runs

# ---------------------------------------------------------------------------
# AWS integration (optional but recommended)
# pipeline-aws provides withAWS{} blocks; not used in current Jenkinsfile
# but needed if you add AWS steps later (CloudFront invalidation, S3 upload)
# ---------------------------------------------------------------------------
pipeline-aws:latest
